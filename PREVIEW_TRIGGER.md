# Cloudflare Preview Trigger

This file exists only to trigger a non-production Cloudflare Workers build for the `astro-haze-migration` branch after reconnecting the Git integration.

It has no effect on the production site or Astro output.
