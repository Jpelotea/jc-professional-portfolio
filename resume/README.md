# Public résumé

The stable public résumé URL is `/resume/jc-pelotea-resume.pdf`.

The PDF is versioned with the portfolio release while the public URL remains stable. Update the source document separately, regenerate an accessible/selectable-text PDF, and replace the PDF only through a reviewed pull request.
